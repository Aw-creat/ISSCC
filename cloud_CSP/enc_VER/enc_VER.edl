enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
		public void foo_th([out,size=len] char* buf,size_t len);

    };

    untrusted {
        /* define OCALLs here. */

    };
};
